"""Reusable Streamlit render components (plan section 3).

Each component takes an engine object (Analysis / MatchBoard) plus the ``st``
module and renders it. Components never compute betting numbers.
"""
